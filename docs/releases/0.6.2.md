# 0.6.2

The setup and front-door terminal wordmark now says `tested plumbing for
plugins`, exactly matching the README banner GIF and its recorded positioning.
The previous runtime-only sentence is removed.

Release verification also allows npm nearly five minutes to expose a version
after accepting it. The 0.6.1 publish remained in npm's processing state past
the former six integrity probes over 50 seconds, then completed unchanged; the
new bound is 30 probes ten seconds apart, still requiring the exact prepared
tarball integrity before the GitHub Release is created.

Upgrade with:

```bash
omakit upgrade
```
